# RBAC test artifacts

This directory is reserved for RBAC-specific test artifacts so they do not get mixed with broader smoke, e2e, or platform behavior notes.

Current artifacts:

- [`role-permission-test-matrix.md`](./role-permission-test-matrix.md) — source-of-truth matrix for role/permission allow-deny coverage.

When implementing RBAC tests in a separate chat or PR, link back to the matrix first and keep new RBAC-only test notes in this directory.
