# Optional: gateway public origin for dev CORS headers in next.config.ts (defaults to http://localhost:3000).
# GATEWAY_ENTRY_ORIGIN=https://proflow.local

# Dev: `true` restores full Next.js incoming request logs. Default: mutating-only logs via proxy.
# DEV_FULL_REQUEST_LOG=true
