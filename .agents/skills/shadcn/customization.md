# Customization & Theming

## Pyramid Layer

- Layer: L2 customization router.

## Use This When

- Load this after the shadcn router when the task is specifically about presets, CSS variables, theming, or component customization.
- Use this file to choose the narrowest customization leaf before loading examples.

## Stop Here If

- Stop here once the task is clearly about global theme variables/presets or component-level overrides and updates.

## Descend To

- Theme variables, dark mode, presets, and global theme changes: `/.agents/skills/shadcn/customization-theme-variables-and-presets.md`
- Component overrides, wrappers, and update preview workflow: `/.agents/skills/shadcn/customization-component-overrides-and-updates.md`
- Return to `/.agents/skills/shadcn/SKILL.md` if the task expands into CLI, MCP, forms, or composition rules.

Components reference semantic CSS variable tokens. Change the variables to change every component.

Customization work now splits into two narrow concerns:

1. Global theme variables, dark mode, presets, and color tokens.
2. Component-level overrides, wrappers, and update preview workflow.

Load only the matching leaf above.
